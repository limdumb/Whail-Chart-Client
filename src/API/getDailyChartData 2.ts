import { PlatformValueType } from "../Function/pletformValue";

interface DailyChartProps extends PlatformValueType {
  date: number;
}

/* 
  Platform 별 Response Data Type 정의
  Platform별 if문 정의 

*/
export const getDailyChartData = (props: DailyChartProps) => {

};
